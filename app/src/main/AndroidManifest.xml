<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.myp.meiyuan">>

    <!-- 微信权限-->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <!--分享权限-->
    <uses-permission android:name="android.permission.GET_TASKS" />
    <uses-permission android:name="android.permission.CHANGE_WIFI_STATE" />
    <uses-permission android:name="android.permission.MANAGE_ACCOUNTS" />
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <!-- 蓝牙分享所需的权限 -->
    <uses-permission android:name="android.permission.BLUETOOTH" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" />


    <application
        android:name=".base.MyApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ui.login.LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".ui.main.MainActivity" />

        <activity android:name=".ui.deviceadd.DeviceAddActivity" />


        <activity android:name=".ui.monitormessage.MonitorMessageActivity" />


        <!--控制详情界面-->
        <activity android:name=".ui.controlmessage.ControlMessageActivity" />

        <!--修改大棚名称界面-->
        <activity android:name=".ui.updategreenhouse.UpdateGreenHouseActivity" />

        <!--修改界面-->
        <activity android:name=".ui.updategreenhouse.update.UpdateActivity" />

        <!--操作记录-->
        <activity android:name=".ui.recoder_oper.OperationRecoderActivity" />

        <!--修改设备名称-->
        <activity android:name=".ui.updatedivicename.UpdateDiviceNameActivity" />

        <!--设置-->
        <activity android:name=".ui.SettingActivity" />

        <!--修改密码-->
        <activity android:name=".ui.user_password.User_passwordActivity" />

        <!--用户信息-->
        <activity android:name=".ui.user_message.User_messageActivity" />

        <!--修改设备名称-->
        <activity android:name=".ui.updatedivicename.updatename.UpdateNameActivity" />

    </application>

</manifest>